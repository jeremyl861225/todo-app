-- =============================================================
--  升級：重複排程的「單次改期」
--  用法：Supabase → SQL Editor → New query → 全部貼上 → Run
--  可重複執行，不會動到既有資料。
-- =============================================================

-- 單次改期的例外表：{"原本的日期":"改到的日期"}，例如 {"2026-08-31":"2026-09-01"}。
-- 只影響那一次，排程規則本身不變。
alter table public.schedules
  add column if not exists moves jsonb not null default '{}'::jsonb;
