# Design

視覺決定的紀錄，寫於建置完成之後，描述的是實際做出來的東西。
實作全部在 `index.html` 的單一 `<style>` 區塊；方向契約在 `<body>` 的第一個註解裡（seed `fb03e0a5`）。

## 一句話

這個 App 唯一要回答的問題是「規律嗎、間隔多久、有沒有多出來的一拍」——所以它拒絕待辦類別的標準答案（白底＋圓角卡片＋單一藍色強調＋柔和陰影），改用兩個量測工具的世界說話。

## 三個世界

視覺世界是可切換的，寫在 `<html data-world>`，存 `localStorage.todo.world`，預設 `ecg`。
明暗另外由 `<html data-scheme>` 決定（`localStorage.todo.theme`：`auto`／`light`／`dark`）。
兩個屬性都由 `<head>` 裡的同步小 script 在第一次繪製前寫好，所以不會先閃錯的顏色。

| `data-world` | 名稱 | 材料 |
|---|---|---|
| `ecg`（預設） | 心電圖紙 | 純白的紙、橘紅量測方格、黑墨；深色時換成床邊監視器 |
| `board` | 開刀房排程白板 | 冷灰搪瓷、無方格、2px 粗黑分隔規、反白區段橫條、實色類別膠帶 |
| `paper` | 原設計 | 暖紙＋鼠尾草綠，圓角卡片與陰影（使用者要求保留的後路） |

共六組色票（3 世界 × 明暗）。**元件本身完全不分世界**，只讀語意 token；真正屬於某個世界的結構集中在樣式表最後的「世界差異」段。

## 色彩規則

1. **暖色只存在於量測方格裡。** `ecg` 的紙是純白 `#FFFFFF`，不是米白。把暖度同時放進主色與底色是這一版刻意避開的（前一版就是那樣，被判定「跟原本太相近」）。
2. **訊號紅只有兩種身分**：格線（時間本身）與逾期／今天／重大（緊急）。沒有第三種用途，所以紅色永遠有意義。
3. **動作的顏色是墨，不是彩色。** `ecg`／`board` 的主要動作（FAB、主按鈕、選中的分頁與晶片、勾選框）一律是實色墨黑，深色主題反轉成光。
4. **類別色只負責身分，永遠不當字色。** 使用者自選的顏色深淺不可控，拿它當字一定有幾個過不了對比。它出現在：圓點／方塊（全彩度）、月曆晶片的底與框、節奏的筆畫、白板的膠帶。
   - 深色主題用 `liftForDark()` 把 L<0.62 的類別色提到 0.68、彩度收到 ≤0.5，色相不動。
   - 白板的膠帶用 `tapeColor()` 把底色朝「撐得住壓字」的方向推到 ≥4.6:1 為止。
5. **退場狀態不靠 opacity，也不靠底色。** 整塊 opacity 會把裡面每個標籤的對比一起壓掉（實測掉到 2.0）；底色在深色主題會反過來（`card2` 比 `card` 亮）。訊號是：字轉灰、加刪除線、節奏拉平、標籤改描邊不填色。
6. `color-mix(…, transparent)` 在 sRGB 下不是「同色半透明」，會把顏色往黑拉。要半透明的面一律明寫 `rgba`（`--veil`）。

**驗收**：3 世界 × 明暗 × 4 頁面 × 6 個對話框，正文對比全部 ≥4.5:1（大字 ≥3:1），在瀏覽器裡取 computed color/background、正確堆疊 alpha 後實測。

## 字

系統字堆疊（CJK 沒有可離線自帶的選項；加中文網頁字型會毀掉離線 PWA）。
**所有數字走等寬並開 `tabular-nums`**（`--num`）：日期、時間、次數、完成度、月份、同步狀態。這不是裝飾——它們是量測值。`paper` 世界把 `--num` 退回一般字體。

字級 `--t-2xs` 11.5 → `--t-3xl` 30，桌機頁標題用 `--t-3xl`。每頁只有一個字級上的紀念碑（頁面標題），其餘靠層級與粗細。

## 節奏（這個 App 唯一獨有的視覺）

`cadenceHTML()` 用既有的 `occursOn()` 算出未來 N 天的實際落點，從**今天**起算、每 7 格一組，所以同一個星期幾永遠落在同一個位置。

- `ecg`：`traceHTML()` 畫成心律條。`viewBox="0 0 n 20"` ＋ `preserveAspectRatio="none"`，x 被拉開十幾倍、y 不變，波形因此又窄又高——25 mm/s 的 QRS 本來就長這樣。
  **筆畫一定要 `vector-effect:non-scaling-stroke`**，否則垂直線（週界、今天的記號）會被 x 縮放一起放大成寬色塊。
- `board`／`paper`：`stripHTML()` 畫成方塊，貼到的日子實色、沒貼到的空格。
- 已結束的排程：`ecg` 拉成平線（用 `--line` 不是 `--tick`，後者在白底幾乎看不見），其餘世界方塊轉灰。

卡片上四週，詳細內容裡六週（再多格子就太細）。

## 版面

- 沒有卡片。`ecg`／`board` 的項目是紙上／板上的橫列，分隔只由規線負責。只有 `paper` 把卡片與陰影還回去。
- 桌機把分組排成並列的欄：`repeat(auto-fill, minmax(280px,1fr))`。
  **`auto-fill` 不是 `auto-fit`**——只剩一組時（例如「已完成」只有一個月份）`auto-fit` 會把那一欄拉成整個版面寬，卡片變成 1200px 長條。
- 版面寬 1240。月曆在 ≥940px 分成月視圖＋當日面板；晶片一格最多 3 筆（`CELL_CHIPS`），大螢幕多出來的寬度拿去讓標題讀得完整，不是多塞一列——多一列會撐破格子高度被裁掉。
- **月視圖的結構是使用者指定不可更動的**，三個世界只換材料。`ecg` 的月曆自己的格線（`--g-cell`）必須比底下的量測方格強，否則月視圖會溶進紙裡。

## 動態

只有一個被刻意設計的瞬間：**勾完成的那一下**——勾選框回彈（`ck-pop`）＋勾勾描繪（`ck-draw`，`stroke-dashoffset`）。
用 `popKey` 只讓「剛剛切換的那一列」帶 `.pop`，否則整頁的已完成項目每次重畫都會一起跳。取消勾選不播。
其餘一律是 140–220ms 的安靜過場。`prefers-reduced-motion` 全域關閉。

## 圖示

全部是畫出來的 SVG（`IC` 表），24 格線、圓端點、統一筆畫。
不用 `★ ▸ ✓ ＋ ←` 這類符號字——它們會落到系統符號字體，粗細與視覺大小跟其他圖示對不上。
`.star` 要用 `inline-flex` 當成 1em 方框讓 SVG 撐滿；用 `inline-block` 時 SVG 會落在基線上、下方留出降部空間，看起來就偏高。

## 瀏覽器自己畫的部分

文字選取色、輸入游標（`--hot`）、捲軸、focus ring、`accent-color`、等寬數字，全部指定過。
`<select>` 的箭頭是 data URI，吃不到 `currentColor`，明暗各一份（`--sel-arrow`）。
日期欄位沒填值時預設置中，要 `text-align:left` 才跟填了值的欄位對齊。

## 不做的事

- 不加中文網頁字型（離線 PWA）。
- 不用 `window.prompt`／原生對話框做輸入（已全數改成就地輸入列）；`confirm()` 只留給刪除。
- 不用整塊 opacity 表示狀態。
- 不讓任何資訊只靠顏色傳達。
